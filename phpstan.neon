includes:
    - vendor/nunomaduro/larastan/extension.neon

parameters:
    level: 5

    paths:
        - app
        - routes

    inferPrivatePropertyTypeFromConstructor: true

    larastan:
        enable_model_property_inference: true